package cli;


public class CLI {
	/*
	public static void main(String[] args) {
		// Array<Integer> combos = new Array<>();
		
		Scanner sc = new Scanner(System.in); 
		// Attend in a step of 5
		for (int i = 0; i < 5; ++i) {
			Clientes.addToClientes(i);
		}
					
		Clientes.printClientes();
		System.out.println(Clientes.isClientesEmpty());
		
		for (int i = 0; i < 5; ++i) {
			System.out.println("Nombre del cliente " + Clientes.clientesCola.peek());
			String clientName = sc.nextLine();
			Clientes.attendCliente(clientName);
		}
		
		// Print clients waiting in grocery store
		 Clientes.printClientesDulceria();
		 
		 Clientes.attendClienteDulceria();
		 
		 Clientes.printClientesDulceria();
		 		 
		 Clientes.printClientesPremium();
		
		
		// Close the scanner object
		sc.close();
		
	}*/
}

// Array class
/*
class ArrayOfCombos {
	
	public static void initializeArray() {
		int[] combosArray;
		combosArray = new int[5];
		int i = 0;
		do {
			combosArray[i] = i;
			i += 1;
		}while(i < 5);
		
		for (i = 0; i < 5; ++i) {
			System.out.println(combosArray[i]);
		}
	}
	
	
}
*/

